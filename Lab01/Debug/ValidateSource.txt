
post2nfa 函数的调用信息：正则表达式到 NFA 。

数据信息：

正则表达式(regexp)：
	a*

解析树的后序遍历序列(postfix)：
	a*


NFA 片段栈(FragmentStack)：

---------------------------------------------------------------------------

post2nfa 函数的返回信息：返回 NFA 的开始状态地址。

数据信息：

正则表达式(regexp)：
	a*

解析树的后序遍历序列(postfix)：
	a*


返回的 NFA ：
	 3(0)->ε-> 1
	 3(0)->ε-> 4
	 1(0)-> a-> 2
	 2(0)->ε-> 1
	 2(0)->ε-> 4
	 4(1)
	-------------
NFA 片段栈(FragmentStack)：

===========================================================================
